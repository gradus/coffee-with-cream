# flatiron and coffeecup example with creamer

## usage

```
npm install .
node server.js
```

This app currently requires the master version of director:

```
cd node_modules
cd flatiron
npm uninstall director
npm install git://github.com/flatiron/director.git
```
